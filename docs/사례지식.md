# 사례 지식 — 착한가격 지도

> 2026-09-19 ~ 09-21(3일) 기획부터 배포·실기기 확인까지, 이후 운영 개선(09-22~)까지 대화 전체를 되짚어 **다른 프로젝트에서 재사용할 지식**만 추렸다.
> 서비스 자체보다 **데이터 인사이트 · 데이터 리터러시 · 설계 · 디자인 · UX · 배포 · 기능 설정** 사례로 본다.
> 각 항목은 **원칙(재사용할 것) → 이번 근거(수치·사례)** 순서. 세부 오류는 [버그이력](버그이력.md), 설정 절차는 [배포설정기록](배포설정기록.md).
> 다른 업무에서도 계속 쓰는 주제는 **모듈 문서**로 따로 두고 여기서는 요약과 링크만 둔다(장 제목은 그대로 유지): [분류·검색 매핑](분류-검색매핑.md) · [공공데이터 파이프라인](공공데이터-파이프라인.md) · [외부 플랫폼 레퍼런스](외부플랫폼-레퍼런스.md) · [지식화 프로세스](지식화-프로세스.md).
> 문서끼리 어긋나지 않았는지는 `python check_docs.py`로 확인한다.

---

## 0. 한 장 요약

| 구분 | 핵심 교훈 |
|---|---|
| 데이터 인사이트 | 공공데이터 원본은 **출발점일 뿐**. 같은 기관의 다른 창구(공식 누리집)에 더 풍부한 데이터가 있었다(좌표·사진·영업시간·전체 메뉴) |
| 데이터 리터러시 | "검색에 안 나옴 ≠ 폐업". 영세·고령 업소는 온라인에 없다 → **행정·세무 데이터**가 마지막 퍼즐 |
| 설계 | 공통 키가 없는 데이터 결합은 **점수 기반 매칭 + 단계적 교차검증**. 외부 ID는 믿는 값이 아니라 **다시 확인하는 값**, 내부 ID는 **영구** |
| 디자인 | 식별도는 공식 상징에서, 신뢰는 **출처·비공식 명시**에서. 색은 역할로 나눈다(파랑=신뢰, 노랑=가격) |
| UX | **탐색 순서와 카드 위계는 다르다.** 보여준 뒤 권하고(설치), 먼저 묻지 않는다 |
| 배포 | 무료 조합(GitHub + Cloudflare Pages + 카카오 + 제미나이 무료)으로 **운영비 0원**. 함정은 대부분 "콘솔 설정" |
| 기능 설정 | 규칙 우선 + AI 보조(하이브리드), 링크 연계(크롤링 대신), 실패 시 대체 경로를 항상 둔다 |
| 지식화 | **작업 중 즉시 기록 + 정기 점검(주간→분기) + 사람 승인 후 공개.** 기준 커밋으로 중복 없이 이어서 점검 |

### 0-1. 출발점 — 목적과 포지셔닝
- **목적 정의(판단 기준의 뿌리):** 공식 사이트(goodprice.go.kr)와 **경쟁하는 서비스가 아니라**, 공공정보와 지도를 연계하는 **개인 개발 사례(비영리 포트폴리오)**. 여기서 이후 기준이 모두 나왔다 — 무료 API만, 크롤링 대신 링크 연계, 공식 정보는 출처 표기, 비공식 명시.
- **차별성 점검:** 2025년 7월부터 네이버지도·카카오맵·티맵에 착한가격업소가 이미 표시되고, 공식 사이트·민간 앱도 있다. "지도에 핀 찍기"만으로는 차별점이 없다.
  → 차별점은 **메뉴·가격 중심 탐색**(지도 앱에는 가격이 안 나옴), 여러 채널 후기로 바로 이동, 문장 검색, 데이터 품질(교차검증) 자체.
- **교훈:** 만들기 전에 ① 누구와 경쟁하지 않는지 ② 이미 있는 것 대비 무엇이 다른지를 한 줄로 정하면, 이후 수십 개의 선택(비용·법·디자인)이 흔들리지 않는다.

### 0-2. 진행 기간 (다음 프로젝트 일정 추정용)
| 일자 | 한 일 |
|---|---|
| 1일차 (09-19) | 구성안 검토(API·법·무료 조건·갱신 전략), 공식 사이트 분석, 좌표·데이터 수집과 결합(12,427건), 엑셀 산출물 |
| 2일차 (09-20) | API 발급(카카오·네이버·도로명주소·공공데이터), 카카오 매칭, 네이버·전화·주소·상가정보 교차검증(검수 2,615 → 245), 영업시간·메뉴 수집, 시안, 인트로·AI 문장 검색, 화면 개선 |
| 3일차 (09-21) | 로고·홈·탭 구조 개편, PC 레이아웃, 배포 전 점검 3회(오류 20여 건), 사진 축소본, 배포(GitHub·Cloudflare), 실기기 확인·수정, 문서화 |
| 4일차 (09-22) | 배포 사이트 AI 검색 실측·개선(버그이력 #48~50), 업종 묶음(#51), 지식 모듈 분리 |
- 데이터 작업 약 1.5일, 화면 약 1일, 점검·배포·실기기 약 0.5~1일. 대기 시간이 긴 작업(매칭 30~45분, 사진 축소 1.7시간)은 **백그라운드로 돌리며 다른 일을 병행**했다.

---

## 1. 데이터 인사이트 — 데이터에서 무엇을 발견했나

### 1-1. "공식 오픈데이터"보다 "공식 서비스 화면 뒤 데이터"가 더 풍부할 수 있다
- 공공데이터포털 CSV: 12,645건, 항목은 시도·업종·업소명·연락처·주소·메뉴1~4·가격. **좌표·고유ID·영업시간·사진 없음.**
- 같은 행안부의 공식 누리집(goodprice.go.kr): 카카오맵 기반이라 **업소별 좌표·고유번호·영업시간·편의시설 12종·사진·메뉴 무제한·지정메뉴 여부**가 있었다.
- 수집 부담 측정이 판단을 바꿨다: 예상 250회 → 실제 **시도별 16회 요청**(2초 간격)으로 12,882건 좌표 확보.
- **교훈:** 오픈데이터 스펙만 보고 설계하지 말고, 같은 기관의 서비스 화면이 무엇을 쓰는지 먼저 본다. 단 robots.txt·부담·권리(후기·사진)를 함께 확인.

### 1-2. 결합 결과 자체가 인사이트
| 결합 | 건수 | 읽을 거리 |
|---|---|---|
| 업소명+주소 완전 일치 | 11,859 | 같은 행정 시스템 출처라 대부분 그대로 맞음 |
| 업소명 일치+같은 시군구 | 342 | 지번↔도로명 표기 차이, 일부는 **실제 이전** 후보 |
| 주소 일치+업소명 유사 | 226 | 상호 표기 차이 |
| CSV에만 | 218 | 지정 취소 가능성 |
| 사이트에만 | 473 | CSV 기준일 이후 **신규 지정** |
- **교훈:** 두 출처의 차집합은 "오류"가 아니라 **시간 차(신규·취소)** 를 드러낸다. 기준일을 항상 같이 본다.

### 1-3. 플랫폼 등록률은 업종마다 다르다
- 카카오 장소 매칭률: 중식 84.0%, 양식 82.9%, 한식 82.7% ↔ **세탁 62.7%, 미용 64.8%, 목욕 66.1%, 이용 69.6%**.
- 카카오에 없던 2,258건 중 **1,528건이 네이버에는 있었다** → 동네 미용실·세탁소는 네이버 플레이스 쪽 등록이 많다.
- **교훈:** 단일 플랫폼 매칭률로 데이터 품질을 판단하지 말 것. 업종별로 보완 채널이 다르다.

### 1-4. 영세 업소의 "온라인 부재"를 잡은 것은 행정·세무 데이터
- 검수 단계별 확정: 카카오 10,503 → 네이버 +1,667 → 전화번호 +289 → **소상공인 상가정보(국세청·카드사) +414** → 남은 **245건(1.9%)**.
- 남은 245건도 82.4%는 공식 사이트에 지정 업소로 등록 → 폐업보다 **상호 변경·온라인 미등록** 가능성.
- 판단(사용자): 245건은 포털·후기·사진이 모두 없어 **노력 대비 실익 없음 → 중단**.
- **교훈:** "검색에 안 나온다 = 폐업"은 고령·영세 업소에서 틀린다. 실재 확인의 최종 근거는 **행정·세무·카드 매출** 계열 데이터.

### 1-5. 원천 데이터 오류 유형 (다른 공공데이터에도 흔함)
- 일괄 치환 사고·오타·행정구역 개편 미반영·시군구 칸 도로명·자정 넘김 영업시간·가격 이상치, 존재하지 않는 주소 1.8%(241건). 우리는 보정·표시만, 원천 수정은 공식 창구로. → [공공데이터 파이프라인](공공데이터-파이프라인.md) 3장

### 1-6. 데이터로 확인한 사용자 기준값
- 가격 분포(음식점): 3천 원 이하 16.0%, **5천 원 이하 30.8%**, 7천 원 이하 55.0%, 1만 원 이하 85.5%, 중간값 7,000원 → 가격 필터를 5천 원 고정 대신 **3천·5천·7천·1만 선택형**으로.
- 고유 메뉴명 8,056개, 상위 키워드 23개로 56.5%만 커버 → **품목 콤보박스 불가, 업종 칩 + 품목 검색**.
- 영업시간 보유 57.4%(해석 성공 97%) → "지금 영업 중"은 **등록 업소 기준**임을 표시.
- 사진 보유 94.8% → 목록 사진 중심 설계가 가능.

---

## 2. 데이터 리터러시 — 데이터를 어떻게 읽고 판단했나

### 2-1. 공통 키가 없을 때의 매칭 사고법
- 거리 + 이름 유사도 + 전화로 점수화, **등급을 거리로 재분할**(LOW 562건 중 30m 이내 82%는 같은 가게), 싼 채널부터 교차검증(2,615 → 245). → [공공데이터 파이프라인](공공데이터-파이프라인.md) 4장

### 2-2. 사업장명과 간판명은 다르다 (사용자 통찰)
- 사업자등록 상호 ≠ 간판(까꼬뽀꼬미용실 ↔ 까꼬뽀꼬헤어샵) → 근거리면 LOW도 채택, 전화가 같으면 같은 업소. → [공공데이터 파이프라인](공공데이터-파이프라인.md) 4장

### 2-3. 외부 ID의 성격을 구분한다
- 우리 관리번호는 영구, 플랫폼 ID는 확인일과 함께 저장·재확인. 순번 ID 노출로 번호가 밀릴 뻔했다(버그이력 #29). → [공공데이터 파이프라인](공공데이터-파이프라인.md) 5장

### 2-4. 좌표 하나의 역할을 과대평가하지 않기
- 좌표는 같은 위치에 핀을 찍게 할 뿐, 가게 페이지 연결에는 플랫폼별 장소 ID가 필요(좌표는 검색 범위를 좁히는 기준점). → [공공데이터 파이프라인](공공데이터-파이프라인.md) 4장

### 2-5. 품질 지표는 여러 개로 나눠 본다
- 하나의 정확도 대신 **무엇이 어떤 근거로 확정됐는지** 등급으로(좌표 100%, 카카오 ID 80.0%, 검수 1.9% 등). → [공공데이터 파이프라인](공공데이터-파이프라인.md) 7장

### 2-6. 검증은 두 경로로
- 엑셀은 재계산 + Python 독립 계산 대조, 앱 데이터는 `check_data.py` 불변조건 자동 검사. → [공공데이터 파이프라인](공공데이터-파이프라인.md) 7장

---

## 3. 설계 — 구조와 판단

### 3-1. 전체 구조: 전처리형 정적 앱 + 얇은 서버 기능
```
[분기 1회, PC]  공공데이터 + 공식 누리집 → 결합 → 좌표·카카오 매칭 → 네이버·전화·주소·상가정보 검증
                → 앱 데이터(시도별 JSON, 8MB) + 사진 축소본(12,429장, 198MB) + 번호 대장
[배포]          정적 사이트(app/) + 서버 기능 3개(functions/: AI 중계, 공식 소식, 공유 미리보기)
[브라우저]      필터·거리 계산·정렬 전부 로컬, 위치는 기기 안에서만
```
- 앱이 외부 API를 실시간으로 부르지 않는다 → 빠르고, 한도·비용·키 노출 걱정이 적다.
- 분기 갱신은 **이전 분기와 비교해 바뀐 것만** 처리(유지·정보변경·이전·상호변경 의심·신규·제외).

### 3-2. 플랫폼 선택 논리 (무료 조건)
- 카드 없이 쓸 수 있는 조합: 카카오(지도·좌표·매칭), 네이버 API HUB(검증), 구글은 링크만, 도로명주소·상가정보(공공), 제미나이(AI), Cloudflare(배포). → [외부 플랫폼 레퍼런스](외부플랫폼-레퍼런스.md) 1장

### 3-3. "크롤링 대신 링크" 원칙
- robots.txt 차단은 멈춤 신호, 자동 수집 대신 **공식 API로 애매한 것만 골라내고 사람은 링크로 확인**, 후기·사진은 채널 바로가기. "허용된 방식만으로 만들었다"가 공개 사례의 강점. → [외부 플랫폼 레퍼런스](외부플랫폼-레퍼런스.md) 2장

### 3-4. 사진 정책의 진화
1. 공식 사진을 원본 주소로 연결(보관 안 함) → 목록 사진이 평균 1.1MB(최대 5.3MB)라 느리고 공식 서버 시간초과.
2. 화면에 보이는 것만, 동시 6개 제한 → 개선됐지만 여전히 무거움.
3. **목록용 360px 축소본을 미리 생성해 함께 배포**(평균 15KB, 약 70배 가벼움), 상세는 원본 + 축소본 배경. 사용자 결정으로 "링크만" 원칙을 목록용 축소본에 한해 조정.

### 3-5. AI는 "문장 → 필터 조건"만
- 업소 데이터를 AI에 보내지 않는다. AI는 문장을 **JSON 스키마로 강제된 조건**으로 바꾸고, 검색은 앱이 로컬에서 한다 → 토큰 적음, 데이터 유출 없음, 모델 교체 쉬움.
- **규칙 해석 먼저, 못 읽을 때만 AI**(하이브리드) → 대부분 0원·즉시, 무료 한도 보호. 실제로 테스트 중 제미나이 503이 나와 대체 경로의 필요성을 확인.
- 모델 선택: 무료 등급·카드 불필요로 **제미나이**. 클로드는 이중화 후보였으나 비상업 단계라 보류(키만 넣으면 켜지게 설계).
- AI 응답은 **허용 목록으로 재검증**(없는 업종·시설명 무시, 가격 범위 제한).
- **배포 사이트에서 실측해 보강한 것**(버그이력 #43~46): 무작위성 0, 세부분류 검색어 → 세부 칩, 일시 오류 1회 재시도, 8초 제한, 최신 검색 답만 적용 → 상세는 [분류·검색 매핑](분류-검색매핑.md) 원칙 17~19. 무료 등급이라 테스트 요청은 간격을 두고 보냈다.

### 3-6. 분류 체계 × 자연어 검색 × AI 매핑 → 모듈 문서
- 다른 업무에서도 계속 쓰는 주제라 **[분류·검색 매핑](분류-검색매핑.md)** 으로 분리했다(층 구조, 원칙 24개와 버그 번호 근거, 현재 매핑 표, 다른 프로젝트 적용 순서).
- 요약: ① 데이터 값은 그대로, 분류는 화면 층에서 묶고 매핑 표는 한 곳 ② 업종 말과 메뉴 말을 나누고, 사용자가 말하지 않은 조건은 짐작하지 않으며, 입력한 단어는 버리지 않는다 ③ AI는 데이터 값으로 답하고 앱이 재검증·변환 → 분류를 13개에서 8개로 바꿨어도 AI·서버는 그대로 ④ 결과 0곳은 조건을 풀어 대안 제시, 회귀 문장은 진입점·처리 경로별로.

### 3-7. 번호 대장(영구 ID) 설계
- 판별 키(공식 업소번호 → 시도+업소명+주소), 한 번 준 번호는 영구·재사용 금지, 모의 테스트로 13,117곳 번호 유지 확인. → [공공데이터 파이프라인](공공데이터-파이프라인.md) 5장

### 3-8. 좌표 보정 절차
- 자동으로 오류 찾기(경계 밖·200m 이상 차이·같은 좌표 다수) → 주소검색 → 장소검색 → 공공 좌표, 출처·신뢰도·확인일 기록, 수기 보정은 별도 파일. 좌표 100% 확보. → [공공데이터 파이프라인](공공데이터-파이프라인.md) 6장

### 3-9. 검수 운영 설계 — 사람이 볼 것만 남기기
- 자동 보정 → Claude 1차 판단(근거·링크) → 사용자 최종 결정 → 보정 파일 반영. 남은 245건은 실익 없어 중단. → [공공데이터 파이프라인](공공데이터-파이프라인.md) 7장

### 3-10. 분기 갱신 설계
- 이전 분기와 비교해 유지·정보변경·이전·상호변경 의심·신규·제외로 나눠 바뀐 것만 처리, 원본 이상(10% 넘게 급감 등) 감지 시 반영 중단. → [공공데이터 파이프라인](공공데이터-파이프라인.md) 8장, 실행 순서는 [운영절차](운영절차.md)

### 3-11. 검토로 확인한 "안 되는 것"과 대체안 (같은 조사 반복 방지)
- 평점·후기 통합과 배달앱 연동은 불가(대체: 채널 바로가기·후기 피드 설계, 배달·포장 배지), 플랫폼별 링크 형식표, 사진 출처 판단. → [외부 플랫폼 레퍼런스](외부플랫폼-레퍼런스.md) 3·4장

---

## 4. 디자인

### 4-1. 브랜드·상징
- 자체 엠블럼은 **식별도가 낮다**(사용자 판단) → 공식 "착한가격" 워드마크를 쓰되 **지자체명·정부상징은 제거**(전국 서비스에 특정 지자체 표찰은 사실관계 오류).
- 행정안전부는 **텍스트로만**("행정안전부 착한가격업소 공공데이터 기반"), 정부상징(태극)은 사용 지침 대상이라 미사용.
- "비공식 개인 서비스" 표기는 인트로·더보기에 유지 — 이유는 저작권이 아니라 **오류 책임·문의 창구 혼동 방지**.
- 표찰 실물 사진은 "착한가격업소란?" 설명 화면에서 출처와 함께.

### 4-2. 색
- 공식 스티커 파랑(라이트 #1241BC, 다크 #2F63E0로 밝게 보정) = **신뢰**, 노랑/앰버 = **가격**(이 앱만 보여주는 정보), 초록 = 영업 중.
- 색은 토큰으로 관리, 라이트·다크 동시 설계. 카카오 지도는 다크 테마가 없어 **지도는 밝게, 주변 UI만 다크**.
- 배지를 이미지로 만들면 다크 대응이 어렵다 → 라이트/다크 두 벌 또는 텍스트 배지.

### 4-3. 글꼴·숫자
- Pretendard(무료·한영 조화, 공식 누리집과 같은 계열), 굵기 3단계(400/600/700).
- 가격은 **고정폭 숫자**, 굵게+강조색, 단위(원)는 작고 흐리게.
- 한글은 줄 간격 1.5 정도로 여유, 다크에서는 글자가 가늘어 보이는 점 보정.

### 4-4. 목업 → 결정 → 적용
- 모바일 목업(홈 둘러보기 / 지도 비교), PC 목업 A(격자형 홈)·B(바로 지도)를 **실데이터로** 만들어 보고 결정.
- 로고도 라이트·다크 샘플 페이지로 비교 후 적용(인트로 배경 투명도 12%·20%·33% 중 20%).

---

## 5. UX

### 5-1. 탐색 순서 ≠ 카드 위계 (사용자 통찰로 교정)
| 구분 | 순서 |
|---|---|
| 탐색(필터) | **위치 → 품목(업종) → 거리 → 가격** |
| 화면 층위 | **지역 → 업종 → 필터 → 검색**(검색은 좁히는 수단이라 목록 바로 위) |
| 카드 표시 | 품목+가격(가장 크게) → 업소명 → 거리·업종·영업 |
| 영업 여부 | 위계가 아니라 **토글 필터** |

### 5-2. 콤보박스 최소화
- 공식 누리집처럼 콤보박스를 늘어놓지 않는다: 평소엔 **지역 2개 + 업종 칩 + 빠른 필터 몇 개**, 나머지는 [필터] 안에.
- 2차 칩은 한식(7,700여 곳, 공식 세부분류 그대로)과 **업종 묶음 3개**(카페·빵·미용·이발·생활 — 원래 업종이 세부 칩, 2026-09-22). 업종 칩 13개 → 8개, 모바일 홈 아이콘은 4개씩 2줄로 넘김 없이. → [분류·검색 매핑](분류-검색매핑.md)

### 5-3. 검색 입력의 두 모드
- **단어(띄어쓰기 없음)**: 타이핑하는 대로 즉시 거름.
- **문장(띄어쓰기 있음)**: 자동 조회하지 않고 "Enter를 누르면 문장으로 검색" 안내 → Enter 시 지역·업종·가격 자동 적용, 검색창엔 핵심어만 남김.

### 5-4. 모바일과 PC는 "같은 코드, 다른 배치"
- 모바일: 지도 + 아래 목록 시트(작게·중간·크게, 지도 끌면 자동 작게), 아래 탭 5개(홈·지도·저장·최근·더보기), 상세는 전체 화면+상단 고정 뒤로가기.
- PC: 왼쪽 세로 메뉴, 조작 영역 2줄, 목록·지도·상세 3단, **바로 지도로 시작** + 목록 위 추천 1줄(접이식), 마우스 올림 → 지도 말풍선(PC만), 키보드 ↑↓·Enter·Esc, 휠로 가로 묶음 이동.
- 태블릿(761~1099px)도 따로 확인.
- **휴대폰 글씨 크기 확대**도 테스트 조합에 넣는다. 가로 배치 속 입력칸은 기본 최소 폭이 글자 폭이라, 큰 글씨에서 버튼을 화면 밖으로 밀어냈다(실기기 스크린샷으로 발견, #42) → 입력칸 `min-width:0`, 화면 고정 오버레이는 내용이 넘치면 **스크롤**되게, 예시 칩은 줄바꿈 허용. 글자 15·22·28px로 실측.

### 5-5. 에어비앤비 참고 — 가져올 것과 아닌 것
- 가져옴: 위쪽 검색 하나 + 아래 탭, **홈의 주제별 가로 추천 묶음**, 업종 아이콘 칩.
- 안 가져옴: 검색 결과의 가로 넘김(둘러보기엔 좋지만 **비교**엔 불리) → 결과는 세로 목록 + 지도.
- 평점·인기 데이터가 없으므로 추천 묶음은 **사실 기반 주제**(5천 원 이하, 지금 영업 중, 이 지역 많은 메뉴, 포장, 주차, 미용·이발). 전체일 땐 음식점 기준.

### 5-6. 위치
- 권한은 **버튼을 누를 때만** 요청, 거부하면 "지도에서 위치 지정"으로 안내, 설정 경로는 더보기에.
- PC는 GPS가 없어 수 km 틀림 → 지도 클릭 지정 우선, GPS 오차 3km 초과 시 확인.
- 기준 위치가 없으면 거리 숨김·"기본순" 표시(0m 표시는 오해).
- GPS 후 **시도·시군구까지 자동 전환**(사용자 실기기 피드백으로 보강).

### 5-7. 설치(홈 화면 추가)·공유
- **설치를 먼저 요구하지 않는다**: 링크로 온 사람에게 가게부터 보여주고, 업소를 본 뒤 **한 번** 제안(7일 유예), 설치했으면 숨김. 첫 화면 가로막는 창은 이탈·검색 불이익.
- 설치 후 사용자 평: "화면 시안성이 더 좋다" → 설치 유도 버튼을 잘 보이게(홈 첫 화면, 더보기 맨 위).
- 브라우저별 안내(크롬·웨일·삼성·엣지·파이어폭스·사파리), 카카오톡 안 브라우저는 "브라우저로 열기".
- 공유: 카카오톡 공식 공유(업소명·가격·사진 카드) + 기본 공유/주소 복사. 링크를 열면 해당 업소 상세가 바로.

### 5-8. 작은 UX 원칙들
- 항상 떠 있는 메뉴보다 **필요할 때만 나타나는 해제 버튼**(기준 위치 ✕).
- 닫기 경로(✕·목록 버튼·휴대폰 뒤로가기)는 **하나의 경로**로.
- 사진 전에는 업종 아이콘을 먼저, 사진이 오면 덮기.
- 정보가 없다고 숨기지 않는다(영업시간 미확인도 목록에 남김).
- **함께 보이는 값은 한 번에 고른다:** 카드의 가격과 메뉴 이름을 각각 다른 곳(가장 싼 가격, 첫 메뉴)에서 가져와 27.6%가 어긋났다. 대표값은 한 함수로 한 쌍을 고르고 카드·정렬·필터·추천·공유가 모두 같은 기준을 쓴다. 가격 앱이라면 대표 가격은 곁메뉴가 아니라 **제도가 보증하는 값**(착한가격 지정 메뉴)으로(#56).
- **정렬은 상황에 맞는 기본값 + 선택:** 위치가 있으면 거리순, 없으면 가격 낮은순("기본순"처럼 의미 없는 순서를 기본으로 두지 않음). 직접 고른 정렬은 유지하고, 위치를 정할 때만 거리순으로 한 번 자동 전환. 평점이 없는 앱에서 쓸 수 있는 정렬은 거리·가격·지금 영업 중(#56).
- **결과 0곳은 막다른 길**: 조건을 하나씩 풀어(검색어 → 가격 → 시설·영업중 → 세부분류) 다시 찾고 **무엇을 뺐는지 알려준다**("'조개찜' 조건은 결과가 없어 뺐어요"). 사용자는 빈 화면 대신 가까운 결과를 받는다(#43).

### 5-9. 첫 화면·공유·개인정보
- **대화형 인트로:** 첫 방문에 공식 로고와 함께 "어디서 무엇을 찾고 계신가요?" 말풍선과 예시 문장(강남에서 5천원 이하 점심 등) → 문장 한 줄로 지역·업종·가격이 적용된 지도로 이동. 세션당 1회, "둘러보기"로 건너뛰기.
- **공유 주소 설계:** `?s=시도코드&id=관리번호`. 상세를 열면 주소창에 id가 붙고 닫으면 빠짐 → 새로고침·즐겨찾기·공유가 모두 같은 가게로. 받은 사람은 인트로를 건너뛰고 해당 상세로 바로. 없어진 업소면 안내 후 기본 화면.
- **로그인 없는 개인화:** 저장·최근 본 업소·화면 모드는 **기기 안에만** 저장(서버 전송 없음). 위치도 기기 안에서 거리 계산에만. → 개인정보 처리 부담이 거의 없고, 안내 문구도 단순해진다.

---

## 6. 배포

### 6-1. 무료 구성과 선택 이유
- GitHub Pages만으로는 **AI 키를 숨길 곳이 없다** → Cloudflare Pages(정적 + Functions)로 한 곳에서 해결.
- Render 무료(잠들면 첫 접속 30초+)·Oracle 무료 VM(카드 필요)보다 **서버 관리 없음·항상 빠름·카드 없음**.
- 결과: 계정 2개(GitHub·Cloudflare) + 콘솔 설정 몇 곳, **운영비 0원**.

### 6-2. 배포 전 점검이 가장 효과적이었다
- 3회 점검에서 오류 20여 건을 배포 전에 잡음(지역 오인식, 데이터 이상, 성능, 저장소 차단, ID 밀림).
- 점검 순서: 기능 흐름 → 문장 회귀 → **데이터 이상치** → 환경(저장소 차단·다크·태블릿) → 성능 측정 → **운영(분기 업데이트 시 무엇이 깨지나)**.

### 6-3. 배포 후에만 드러난 것
- API 키를 일반 변수로 넣으면 적용 안 됨(비밀로 재등록).
- 공식 누리집이 **클라우드 접속을 차단**(PC에선 되던 수집이 배포 서버에선 실패) → PC 수집본 동봉.
- 카카오톡 공유 링크는 **제품 링크 관리** 등록이 있어야 열림.
- 휴대폰 다크 모드에서만 보이던 로고 가림.
- **교훈:** 로컬 테스트로 안 보이는 것 = 외부 서비스 설정·네트워크 출처·실기기 조합. 배포 직후 체크리스트를 따로 둔다.

### 6-4. 외부 서비스 무료 한도·조건 (2026-09 기준, 바뀔 수 있음)
- 카카오·네이버·도로명주소·상가정보·Gemini·Cloudflare 무료 한도와 조건표(2026-09 기준). 요금·한도는 자주 바뀌므로 적용 직전 공식 문서로 재확인. → [외부 플랫폼 레퍼런스](외부플랫폼-레퍼런스.md) 5장(분기 점검 때 갱신)

### 6-5. 플랫폼은 바뀐다 — "작년 방식"을 믿지 말 것
- 네이버 검색 API의 API HUB 이관, 카카오 콘솔 개편, 인허가 사이트 폐쇄, Cloudflare 비밀 변수. 외부 API·콘솔은 기억이 아니라 현재 공식 문서·실제 화면으로 확인. → [외부 플랫폼 레퍼런스](외부플랫폼-레퍼런스.md) 6장

---

## 7. 기능 설정(구현 패턴) — 다음에 가져다 쓸 것

| 패턴 | 이번 구현 위치 |
|---|---|
| 규칙 → AI 폴백 문장 해석, 스키마 강제, 응답 재검증 | `app/intro.js`, `functions/api/parse.js` — [분류·검색 매핑](분류-검색매핑.md) 4장 |
| 업종 묶음(데이터 값 그대로, 화면에서 묶기)·세부 칩·표시 이름 | `app/app.js` `GROUPS` `matchCat` `catLabel` |
| 결과 0곳 조건 완화·뺀 조건 안내 | `app/app.js` `applyParsed` |
| 지역 사전(줄임말·동명 시군구·시도 우선) | `app/intro.js` `SIDO_ALIAS`, `parseQuery` |
| 좌표 → 행정구역(시도·시군구) 자동 전환 | `app/mobile.js` `moveToRegionOf` |
| 마커 캐시·변경 없으면 다시 안 그림·연속 호출 합치기 | `app/app.js` `renderMarkers` |
| 화면에 보이는 사진만·동시 요청 제한 | `app/pc.js` 이미지 큐 |
| 사진 축소본 사전 생성(동시 3, 이어받기, 잘린 원본 복구) | `gen_thumbs.py` |
| 영구 ID 번호 대장 | `build_master.py` `assign_ids` |
| 데이터 불변조건 자동 검사 → 배포 중단 | `check_data.py` |
| 지식 문서 정합성 자동 검사(링크·버그 번호·모듈 연결·코드 이름) | `check_docs.py` — [지식화 프로세스](지식화-프로세스.md) 4장 |
| 저장소 차단 대비 메모리 대체 | `app/tabs.js` `safeStorage` |
| 공유 미리보기(업소별 OG) | `functions/_middleware.js` |
| 외부 수집 실패 시 저장본 대체 | `functions/api/notices.js` + `update_notices.py` |
| 설치 유도(브라우저별 안내·1회 제안·앱 안 브라우저 탈출) | `app/mobile.js`, `app/tabs.js` |
| 카카오톡 공유(Kakao SDK feed) | `app/mobile.js` `kakaoShare` |
| 다크/라이트 토큰, 자동 모드는 저장 안 함 | `app/app.css`, `app/app.js` `initTheme` |
| 목록 시트 3단계·지도 끌면 접힘 | `app/mobile.js` |

---

## 8. 협업 방식에서 효과적이었던 것 (다음 프로젝트 진행법)

1. **"먼저 프로그램 짜지 말고 검토부터"** — 구성안 문서([개발구성안_검토_v1.md](../개발구성안_검토_v1.md))를 섹션을 늘려가며 확정한 뒤 구현. 공식 누리집 구조는 [goodprice_사이트분석.md](../goodprice_사이트분석.md).
2. **"반영하지 말고 검토 의견 먼저"** — 큰 변경(로고·구조 개편·배포 방식)은 의견 → 목업/샘플 → 결정 → 적용.
3. **수치로 판단** — 가격 분포, 메뉴 키워드 커버리지, 매칭률, 요청 횟수, 속도(ms)를 먼저 재고 결정.
4. **사용자의 현장 통찰이 설계를 바꿈** — 간판명≠상호, 영세·고령 업소의 온라인 부재, 탐색 순서, 공식 로고 식별도, 설치 후 시안성.
5. **법·정책 판단은 사용자가, 근거·위험 목록은 Claude가** — C1~C6은 확인할 곳 목록 → 사용자 판단 기록.
6. **실기기 피드백 루프** — 배포 후 휴대폰 확인 6항목 → 즉시 수정 → 재확인(GPS 시군구, 설치, 공유 링크, 큰 글씨에서 잘린 검색바). 사용자가 스크린샷 한 장과 "확인해줘"를 주면 Claude는 **원인 확인 → 수정 → 배포 사이트 실측 → 발견한 추가 문제를 의견으로 제안 → 승인 후 반영**(AI 검색 확인 요청 한 번에 개선 3건 + 추가 결함 4건).
7. **기록을 남기며 진행** — 버그이력(원인·재발 방지), 배포설정기록, 운영절차, 배포전검토, 이 문서.

---

## 9. 다음 프로젝트에 바로 적용할 원칙 (체크리스트 형태)

- [ ] 목적(누구와 경쟁하지 않나)과 차별점(이미 있는 것 대비)을 한 줄로 먼저 정하기
- [ ] 공개 데이터 스펙 외에 **같은 기관 서비스의 데이터**를 먼저 조사(robots·부담·권리 확인)
- [ ] 결합 전 **기준일**을 맞추고, 차집합을 신규·취소로 해석
- [ ] 매칭은 점수화 + **거리로 등급 재분할** + 싼 채널부터 교차검증
- [ ] 외부 노출 ID는 **처음부터 영구 ID**, 외부 ID는 확인일과 함께
- [ ] 원천 오류는 보정·표시만, 원천 수정은 공식 창구로
- [ ] AI는 조건 변환만, 규칙 우선·AI 보조, 응답은 허용 목록으로 검증
- [ ] AI 호출은 무작위성 0 + 시간 제한 + 일시 오류 1회 재시도 + **최신 요청만 반영**, 실패 시 규칙 결과로
- [ ] 규칙을 고친 뒤엔 회귀 문장을 **어느 경로(규칙/AI)가 처리하는지**까지 확인
- [ ] 검색 결과가 0곳이면 조건을 풀어 대안을 보이고 **뺀 조건을 알려주기**
- [ ] 크롤링 대신 **공식 API + 링크 연계**, 차단 신호(robots·캡차)는 멈춤 신호
- [ ] 탐색 순서와 카드 위계를 분리해 설계, 콤보박스 최소화
- [ ] 화면 점검 = 밝게·어둡게 × 휴대폰·태블릿·PC, **카카오톡 안 브라우저·큰 글씨 설정 포함**
- [ ] 목록 이미지는 축소본, 외부 이미지는 대체 경로
- [ ] 데이터 불변조건 자동 검사 → 실패 시 배포 중단
- [ ] 배포 후 외부 수집·콘솔 설정·실기기 체크리스트 따로 실행
- [ ] 설치·권한·알림은 **보여준 뒤 권한다**
- [ ] 외부 API·콘솔·요금은 **현재 공식 문서**로 확인(이관·개편이 잦음)
- [ ] 좌표·매칭 결과에 **출처·신뢰도·확인일**, 수기 보정은 별도 파일로

---

## 10. 지식화 프로세스 — 작업을 지식으로 남기는 방법

### 10-1. 왜 필요한가
- 긴 대화는 요약되며 세부가 사라진다 → 작업하면서 남긴다. 목표는 다음 프로젝트에서 같은 조사·실수를 반복하지 않는 것. → [지식화 프로세스](지식화-프로세스.md) 1장

### 10-2. 세 겹 구조
- ① 작업 중 즉시 기록(메모 규칙) ② 정기 점검(예약 작업: 안정화 주간 → 분기) ③ 사용자 승인 후 공개. → [지식화 프로세스](지식화-프로세스.md) 2장

### 10-3. 설계 판단
- 시간 + 사건 혼합, 단기 촘촘 → 장기 느슨, 기준 커밋으로 이어서 점검, 로컬 예약 작업, 자기 관리(스스로 종료·재예약), 자동 push 금지. 규칙으로 판정되는 점검은 `check_docs.py`. → [지식화 프로세스](지식화-프로세스.md) 4·5장

### 10-4. 문서 지도 (무엇을 어디에)
| 문서 | 담는 것 | 갱신 시점 |
|---|---|---|
| 사례지식.md | 왜 그렇게 했나 — 원칙과 근거(이 문서), 모듈 문서로 가는 링크 | 정기 점검 |
| **모듈 문서** — 분류-검색매핑.md, 공공데이터-파이프라인.md, 외부플랫폼-레퍼런스.md, 지식화-프로세스.md | 다른 업무에서도 쓰는 주제별 설계(원칙 + 근거 번호 + 적용 순서 + 변경 이력 + 갱신 규칙) | 원칙이 새로 생기거나 바뀔 때, 외부 플랫폼은 분기 점검 때 |
| 버그이력.md | 무엇이 틀렸고 어떻게 막나 + 회귀 테스트 문장 | 버그 수정 즉시 |
| 배포설정기록.md | 어디서 무엇을 설정했나, 함정, 배포 체크리스트 | 설정 변경 즉시 |
| 운영절차.md | 분기 업데이트·제외 요청·장애 대응·키 교체 | 절차 변경 즉시 |
| 배포전검토.md | 결정 사항(A~G)과 확인 목록 | 결정 시 |
| 지식화기록.md | 점검 이력·기준 커밋·업로드 상태 | 점검마다 |
| check_docs.py(저장소 최상위) | 문서끼리·문서와 코드의 정합성 자동 검사 | 문서를 고칠 때마다, 정기 점검 때 실행 |
| design/ | 목업·시안(HTML) — 결정 전에 비교한 화면 | 목업 만들 때 |
| README.md(저장소 첫 화면) | 서비스 소개·구성·문서 목록 | 문서·기능이 늘 때 |
| 개발구성안_검토_v1.md, goodprice_사이트분석.md(저장소 최상위) | 착수 전 구성안 검토, 공식 누리집 구조 조사 | 착수 시(이후 거의 없음) |
| Claude 메모 | 사용자 선호·진행 방식·프로젝트 결정 | 대화 중 |

### 10-5. 전체 대화에서 지식 뽑는 방법 (이번에 쓴 방식)
- 대화 기록에서 사용자 메시지와 답변 본문만 추출 → 결정 지점 파악 → 압축 구간 복원 → 관점별 "원칙 → 근거" → 개인 정보 검사. → [지식화 프로세스](지식화-프로세스.md) 6장

### 10-6. 다음 프로젝트에 적용
- 첫날 문서 지도·메모 규칙, `check_docs.py` 복사, 주제가 커지면 모듈로 분리, 예약 작업 시험 실행, 안정화 후 스킬·지침으로 승격. → [지식화 프로세스](지식화-프로세스.md) 7장

