# 배포 전 검토 — 착한가격 지도

> 배포 구성: **GitHub(코드 보관) + Cloudflare Pages(사이트 + 서버 기능)**. 계정 2개, 서버 관리 없음.
> 표시: ☐ 미완료 / ☑ 완료 · 담당: **사용자** 결정·확인 / **Claude** 작업
> 작성 2026-09-21. 항목을 처리하면 체크하고 결과를 적는다.

---

## A. 결정이 필요한 것 (사용자)

| # | 항목 | 선택지 | 추천 | 결정 |
|---|---|---|---|---|
| A1 | **GitHub 저장소 공개 여부** | ① 공개 ② 비공개 | **공개 + 올릴 범위 제한**(B4 참고). 포트폴리오로 보여줄 수 있음. 비공개도 Cloudflare 배포는 가능 | ☑ 공개(키·원본 데이터·네이버 검증 결과 제외) |
| A2 | **사이트 주소** | ① 무료 `이름.pages.dev` ② 개인 도메인 구입(연 1~2만 원) | ① 먼저 시작, 필요하면 나중에 ② 추가(주소 바뀌면 공유 링크가 달라지므로 일찍 정할수록 좋음) | ☑ `goodprice-map.pages.dev` |
| A3 | **공식 "착한가격" 로고 사용 유지** | ① 유지(비공식 문구 표시) ② 자체 로고로 교체 | 공개 배포 전 C2 확인 결과로 결정. 불명확하면 ② 가 안전 | ☑ ① 유지(C2 사용자 판단) |
| A4 | **사진 축소본 보관 위치** | ① 저장소에 함께(약 12,400개, 100~190MB) ② 사진 없이 업종 아이콘만 | ① (Cloudflare 무료 한도 파일 2만 개 이내). 저장소 공개 시 사진 사본도 공개된다는 점 감안 | ☑ ① 저장소에 포함 |
| A5 | **AI 문장 해석 남용 대책 수준** | ① 결제 미연결 확인만 ② + IP당 분당 제한 ③ + 하루 전체 상한 | ①+③: 결제 미연결이면 비용은 절대 안 생기고, 최악의 경우 무료 한도 소진 → 규칙 해석으로 자동 대체 | ☑ 테스트 단계라 보류(결제 미연결 확인만 권장) |
| A6 | **방문 통계** | ① 안 씀 ② Cloudflare Web Analytics(무료, 쿠키 없음, 개인 식별 안 함) | ② 포트폴리오에 방문 수를 보여줄 수 있음 | ☑ ② Cloudflare Web Analytics |
| A7 | **문의·오류 신고 창구** | ① GitHub Issues ② 이메일 ③ 구글 설문 | ③ 또는 ②(일반 사용자는 GitHub 계정 없음). 지금 앱의 "오류 신고"는 공식 사이트로만 연결됨 | ☑ ② 이메일 kosafmax1@naver.com (더보기·개인정보 안내에 표시) |
| A8 | **업소 삭제·수정 요청 처리 방식** | 업소 주인이 "내 가게 빼 달라"고 할 때 | 데이터에서 제외 목록으로 처리(번호 대장은 유지), 처리 기한 정해 두기 | ☑ 행정안전부 착한가격업소에서 조회되지 않으면 제외 목록 처리(공공데이터 활용 기준 준용) |
| A9 | **검색엔진 노출** | ① 허용 ② 차단 | ① 포트폴리오 목적이면 허용 | ☑ ① 허용(비용 없음) |
| A10 | **시안 파일 공개** | `mockup.html`, `pc-mockup.html`, `logo-sample.html`이 앱 폴더에 있어 그대로 공개됨 | 배포에서 제외(저장소 [`docs/design/`](design/)로 이동해 보관) | ☑ 제외, docs/design/ 보관 |
| A11 | **분기 업데이트 주기·방법** | 공공데이터 갱신 확인 → PC에서 스크립트 실행 → GitHub에 올리면 자동 배포 | 분기 1회, 운영 절차서(D10) 따라 진행 | ☑ 분기 1회, 운영 절차서대로 |

---

## B. 보안·키 관리

| # | 항목 | 현재 상태 | 할 일 | 담당 |
|---|---|---|---|---|
| B1 | 코드에 키가 박혀 있는지 | ☑ 점검 완료: 파이썬·JS·HTML에 키 없음(모두 `.env`에서 읽음) | — | — |
| B2 | 카카오 JavaScript 키 | 화면 코드에 공개(정상 — 도메인으로 보호됨) | ☑ `https://goodprice-map.pages.dev` 등록 완료 | 사용자 |
| B3 | 제미나이 키 | ☑ Cloudflare 비밀(Secret)로 등록 (결제 미연결 확인·키 재발급은 사용자 권장 사항) | Cloudflare 환경변수(Secrets)에 입력. Google AI Studio에서 **결제(빌링) 미연결 확인**, 키 사용 범위를 Generative Language API로 제한 | 사용자 + Claude 안내 |
| B4 | GitHub에 올리면 안 되는 것 | ☑ `.gitignore` 작성 | `.env`, `API키.txt`, `data/raw/`, `data/processed/`의 검증 중간파일(네이버·카카오·전화·주소 검증 결과), 엑셀·xls, `__pycache__/`, `data/thumbs/`(개발용) 제외. **`data/processed/id_registry.json`은 반드시 포함** | Claude |
| B5 | 키가 이미 노출된 적 있는지 | 대화에 키를 붙여넣지 않음(파일로만 관리) | ☑ 업로드 대상 파일 70개를 실제 키 6개와 대조 → 0건 | Claude |
| B6 | AI 응답·사용자 입력 검증 | ☑ 완료(버그이력 #23) | Cloudflare 이식 후 재확인 | Claude |
| B7 | 사진 축소 기능이 남의 사진 서버 공격 도구로 쓰일 위험 | 현재 공식 사진 주소만 허용 | 배포에선 축소본을 미리 만들어 두므로 이 기능 자체가 없어짐 | — |
| B8 | 보안 헤더 | ☑ `app/_headers` 작성 | `_headers`로 기본 보안 헤더(외부 사이트 안에 넣기 금지 등) 추가 | Claude |

---

## C. 법·이용 조건 확인 (사용자 확인 권장)

> **2026-09-21 사용자 판단:** C1(공공데이터 활용 장려·비영리·소상공인 홍보), C2(정부가 홍보 확대 중이며 개인 블로그·사이트 게시가 일반적, 홍보 대상), C3(공개된 주소 기반 위치정보, 정책 신뢰도 향상에 기여하는 비영리 활동), C6(제3자 제공·서버 저장 없음) → 문제 없음으로 판단. C4·C5·C7은 D7 안내 문구와 B4 제외 범위로 처리.

> 법률 판단이 아니라 **확인할 곳 목록**이다. 불명확하면 해당 기관에 문의하거나 보수적으로 처리한다.

| # | 대상 | 확인할 내용 | 확인 방법 | 결과 |
|---|---|---|---|---|
| C1 | 공공데이터포털 "행정안전부_착한가격업소 현황" | 이용허락 범위(출처 표시 조건, 변형·재배포 가능 여부) | 데이터 상세 페이지의 "이용허락범위" 칸 | ☑ 문제 없음(사용자 판단) |
| C2 | goodprice.go.kr 사진·로고·표찰 이미지 | 저작권 정책, 공공누리 유형(출처표시/상업금지/변경금지) — **로고 사용(A3), 사진 축소본(A4)에 직결** | 사이트 하단 "저작권정책" 또는 게시물의 공공누리 마크, 불명확하면 행정안전부에 문의 | ☑ 문제 없음(사용자 판단) |
| C3 | 카카오 지도·로컬 API | 로컬 API로 얻은 장소 ID·좌표를 데이터에 저장해 쓰는 것이 허용되는지, 무료 한도 | 카카오 개발자 운영정책·약관 | ☑ 문제 없음(사용자 판단) |
| C4 | 네이버 검색 API | 검색 결과는 **내부 검증에만 사용**, 앱에 노출·재배포하지 않음 → 공개 저장소에 검증 결과 파일 올리지 않기(B4) | 네이버 개발자 이용약관 | ☐ |
| C5 | 제미나이 무료 등급 | 무료 등급 입력은 구글 서비스 개선에 쓰일 수 있음 → "검색 문장은 해석을 위해 Google AI로 전송됩니다" 안내 필요(D7) | Gemini API 약관 | ☐ |
| C6 | 위치 정보 | 현재: GPS 위치는 **기기 안에서만** 거리 계산에 쓰고 서버로 보내지 않음. 이 방식이 위치기반서비스 신고 대상인지 | 방송통신위원회 위치정보 안내, 불명확하면 문의 | ☑ 문제 없음(사용자 판단) |
| C7 | 개인정보 | 앱은 개인정보를 수집·저장하지 않음(저장·최근은 기기 안에만). Cloudflare는 접속 기록(IP)을 남김 | 간단한 "개인정보 처리 안내" 문구 게시(D7) | ☐ |

---

## D. 배포 작업 (Claude)

| # | 작업 | 내용 | 상태 |
|---|---|---|---|
| D1 | 서버 기능 이식 ① 문장 해석 | `/api/parse` → Cloudflare Pages Functions, 제미나이 키는 환경변수, A5 결정대로 제한 | ☑ `functions/api/parse.js` (로컬 Cloudflare 환경에서 확인) |
| D2 | 서버 기능 이식 ② 공식 소식 | `/api/notices` → Functions + 24시간 캐시(공식 사이트엔 하루 1회만 요청) | ☑ `functions/api/notices.js` (24시간 캐시, 로컬 확인) |
| D3 | 서버 기능 이식 ③ 공유 미리보기 | 업소별 제목·가격·사진을 미리보기에 넣는 기능 → Functions(HTMLRewriter) | ☑ `functions/_middleware.js` (업소별 제목·가격·주소·사진, 로컬 확인) |
| D4 | 사진 축소본 미리 만들기 | 공식 서버에 부담 없게 초당 1건 정도(약 3.5시간), 중단돼도 이어받기. 데이터에 축소본 파일명 필드 추가 → 화면은 정적 파일 사용 | ☑ 12,429장 / 198MB (1장은 원본이 이미지가 아니라 업종 아이콘으로 표시), 동시 3건 약 1.7시간 |
| D5 | 데이터 경량화 | 쓰지 않는 필드 제거: 사진목록 `imgs`(용량의 23.6%), 우편번호 `z`, 데이터등급 `q` → 약 25% 감소 | ☑ 11MB → 8.1MB |
| D6 | 업데이트 후 옛 데이터 방지 | 데이터 파일 주소에 버전(`?v=기준일`) 붙이기, `_headers`로 캐시 기간 설정, 개발용 캐시 금지 해제, 서비스워커 캐시 이름 버전 관리 | ☑ `?v=기준일`, `_headers` (개발용 no-store 해제·SW 버전은 이식 때) |
| D7 | 안내 문구 | 더보기에 "개인정보 처리 안내", "AI 검색 안내(C5)", 데이터 기준일, 문의 창구(A7) 추가 | ☑ 개인정보 처리 안내 페이지, 문의 이메일 표시 |
| D8 | 오류 화면 | 데이터를 못 받았을 때(통신 끊김) 안내 + "다시 시도", 상세 원본 사진이 깨지면 축소본·아이콘으로 대체(현재 깨진 이미지 표시됨) | ☑ |
| D9 | 404 페이지, `robots.txt` | A9 결정대로 | ☑ `app/404.html`, `app/robots.txt` |
| D10 | 운영 절차서 `docs/운영절차.md` | 분기 업데이트 명령 순서, 번호 대장 보존, 검증, 배포, 문제 시 되돌리기 | ☑ `docs/운영절차.md` |
| D11 | 데이터 자동 검사 `check_data.py` | 버그 이력에서 나온 규칙을 자동 검사: ID 유지(번호 대장), 오픈=마감 0건, 시군구 형식, 좌표 국내 범위, 필수값, 지난 분기 대비 증감 보고 → **이상 있으면 배포 중단** | ☑ `check_data.py` (일부러 넣은 오류 3건 검출 확인) |
| D12 | 이식 후 회귀 테스트 | 버그이력 부록 문장 17개 + 주요 흐름(문장 검색, 공유, 저장, 필터, 홈 화면 추가) | ☑ 배포 사이트에서 지역 해석 13문장·단어 4종·AI 문장 검색·상세 열기/뒤로가기 통과, 밝게·어둡게 × 모바일·PC 4조합 확인 |
| D13 | 배포 방식 | GitHub `main`에 올리면 자동 배포, Cloudflare의 미리보기 배포(브랜치별 주소)로 먼저 확인 후 반영 | ☑ `https://goodprice-map.pages.dev` — GitHub `main` push 시 자동 배포 (2026-09-21 첫 배포) |

---

## E. 배포 직후 확인 (사용자 휴대폰 + Claude)

| # | 항목 | 확인 내용 | 결과 |
|---|---|---|---|
| E1 | **카카오톡 안에서 열기** | 공유 링크 미리보기(제목·사진), 링크 열기, 위치 권한, 공유 버튼, 뒤로가기, 전화걸기, 길찾기 앱 연결 | ☐ |
| E2 | 아이폰 사파리 | 홈 화면 추가, GPS, 화면 아래 홈 바 여백, 다크 모드 | ☐ |
| E3 | 안드로이드 크롬 / 삼성 인터넷 | 설치 창, 뒤로가기로 상세 닫기, GPS | ☐ |
| E4 | 네이버·티맵·카카오·구글 길찾기 | 목적지가 제대로 찍히는지 | ☐ |
| E5 | PC 크롬·엣지 | 3단 화면, 키보드, 말풍선 | ☐ |
| E6 | 속도 | 휴대폰 회선에서 첫 화면 시간, Lighthouse 점수 | ☐ |
| E7 | 카카오톡 미리보기 캐시 | 미리보기가 옛것으로 나오면 카카오 공유 디버거에서 초기화 | ☐ |
| E8 | 제미나이 한도 | 하루 사용량이 무료 한도 안인지 | ☐ |

---

## F. 운영 중 깨질 수 있는 곳 (알고 대비)

| # | 상황 | 영향 | 대비 |
|---|---|---|---|
| F1 | 공식 사이트 공지 목록 구조 변경 | 공식 소식 수집 실패 | ☑ 실패 시 숨김 처리됨. 가끔 확인 |
| F2 | 공식 사이트 사진 주소 변경·삭제 | 상세 원본 사진 깨짐 | 목록은 축소본이라 영향 없음, 상세는 D8로 대체 |
| F3 | 제미나이 모델 이름 종료(모델은 주기적으로 교체됨) | AI 해석 실패 | ☑ 규칙 해석으로 자동 대체. 운영 절차서에 모델 교체 방법 기록 |
| F4 | 행정구역 변경(통합·명칭 변경) | 지역 해석 오류 | 분기 업데이트 때 시도 줄임말 사전 확인(버그이력 #16) |
| F5 | 공공데이터 형식(열 이름) 변경 | 변환 스크립트 실패 | D11 자동 검사로 배포 전 차단 |
| F6 | 번호 대장 파일 분실 | 모든 ID 재부여 → 저장·공유 링크 깨짐 | 저장소에 포함(B4) + 별도 백업 |
| F7 | 원본·중간 데이터 분실(`data/raw`, `data/processed`) | 재수집·재검증에 며칠 | 저장소에 안 올리므로 **별도 백업**(외장 디스크·개인 클라우드) |
| F8 | 카카오·Cloudflare·제미나이 무료 정책 변경 | 서비스 중단 가능 | 결제 미연결 유지 → 과금 대신 멈춤. 공지 확인 |

---

## G. 이미 완료된 점검 (참고)

- 코드·데이터 점검 3회, 오류 29건 수정 → `docs/버그이력.md`
- 문장 검색 회귀 테스트 17문장 통과
- PC(1440) · 태블릿(900) · 모바일(375), 밝게·어둡게, 저장소 차단 환경 확인
- 코드 내 키 없음, 보안 안 되는 http 주소 없음, 파일 경로 대소문자 일치

## H. 배포 후 결정 (2026-09-22)

| # | 사항 | 결정 | 다시 볼 때 |
|---|---|---|---|
| H1 | 제미나이 키 재발급(설정 화면 캡처에 노출) | 안정화 후 처리 | 2026-12 |
| H2 | AI 해석 남용 방지(A5) | 사용자가 적어 보류 | 무료 한도 경고·AI 실패가 생기면(Cloudflare 속도 제한 규칙 + 요청 출처 확인) |
| H3 | 원본·중간 데이터 백업 | **비공개 GitHub 저장소** + `backup_data.py`(키 검사) | 분기 갱신 전후 |
| H4 | 도로명주소 API 키 | 다음 분기 갱신 때 운영키로 전환 | 첫 분기 갱신 |
| H5 | 분기 갱신 리허설 | 실제 갱신 전에 한 번 | 일정은 사용자와 확정 |
| H6 | 제미나이 모델 종료 일정 | 분기 점검 예약 작업에서 확인 | 매 분기 |
| H7 | AI가 빠뜨린 조건을 규칙 결과로 채우기 | 적용(버그이력 #52) | — |
| H8 | 문장에 지역이 없으면 보던 지역 유지 | 유지하고 "(보던 지역)" 표시 | — |
| H9 | 후기 탐색 허브(블로그·카페 피드) | 계속 보류. 구현하면 **최근 6개월 게시물만** | 구현 결정 시 |
| H10 | 저장소 라이선스 파일 | 유보(첫 화면·본문에 공공데이터 출처 명시함) | 필요 시 |
| H11 | 검색 노출 | 구글 서치 콘솔·네이버 서치어드바이저 **사이트 등록만**(sitemap.xml 추가) — ☑ 2026-09-22 두 곳 소유 확인·사이트맵 제출 완료([배포설정기록](배포설정기록.md) 2-10) | 구글 사이트맵 상태가 일주일 넘게 "가져올 수 없음"이면 확인 |
| H12 | 방문 통계(A6) | Cloudflare Web Analytics 켜기(사용자) — ☑ 2026-09-22 켜고 재배포, 수집 확인([배포설정기록](배포설정기록.md) 2-11) | — |
| H13 | 실기기 확인 | 크롬으로 확인(웨일은 생략) | — |

