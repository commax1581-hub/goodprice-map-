# 착한가격업소 지도·평판 연계 앱 — 개발 구성안 검토 (v1)

작성일: 2026-09-19 / 상태: 검토안 (코드 작성 전)

---

## 1. 결론 요약

| 항목 | 판단 |
|---|---|
| 공공데이터 오픈API 제공 여부 | **제공됨.** 공공데이터포털 「행정안전부_착한가격업소 현황」(분기 갱신, 약 12,645건) — 파일(CSV) + 오픈API(JSON/XML 자동변환). 이용허락 제한 없음 |
| 좌표(위경도) | **없음.** 주소만 있음 → 자체 지오코딩 필요 |
| 구글 평점·사진·리뷰 | **공식 API로 가능** (Places API New). 단 **DB 저장 금지**(place_id만 영구 저장 가능), 출처표기·Google 로고 필수, 비용 높은 SKU |
| 네이버 평점·사진·리뷰 | **공식 API 없음.** 지역검색 API는 이름/주소/카테고리/링크만. 크롤링은 약관·법적 리스크 → **링크 이동(딥링크) + 블로그검색 API 후기 목록**으로 대체 |
| 카카오 | 평점/리뷰 API 없음. 지오코딩·장소 ID 매칭·길찾기 딥링크 용도로 유용 |
| 권장 구조 | 반응형 웹(PWA) 1개 코드로 모바일/PC 대응 → 필요 시 앱스토어 래핑 |
| 가장 큰 리스크 | ① 공공데이터 ↔ 외부 플랫폼 **업소 매칭 오류**(엉뚱한 가게 평점 노출) ② 구글 API **비용** ③ 기존 서비스와의 **차별성** |

---

## 2. 공공데이터 검토

### 2.1 데이터셋
- 명칭: 행정안전부_착한가격업소 현황 (data.go.kr/data/3045247)
- 제공: CSV 다운로드(로그인 불필요) + 오픈API 자동변환(JSON/XML, 활용신청 필요)
- 갱신: 분기 / 차기 등록 예정 2026-10-30
- 건수: 약 12,645행
- 컬럼: `시도, 시군, 업종, 업소명, 연락처, 주소, 메뉴1~4, 가격1~4`
- 없는 것: **위경도, 업소 고유ID, 영업시간, 사진, 지정일, 폐업여부**
- 별도로 각 지자체(의정부시, 용인시 등)가 자체 데이터셋을 올림 → 컬럼·기준일이 제각각이라 **행안부 통합본을 기본 소스**로 권장, 지자체본은 보조 검증용.

### 2.2 호출 방식 판단
- 앱이 사용자 요청마다 data.go.kr API를 직접 부르는 방식은 **비권장**
  - 자동변환 API는 일일 호출 한도·응답 속도·장애 영향을 그대로 받음
  - 좌표가 없어 어차피 가공이 필요
- **권장: 배치 수집(ETL) → 자체 DB 적재 → 앱은 자체 API만 호출**
  - 분기 1회(+월 1회 변경 감지) 전체 수집
  - 신규/변경/삭제(지정 취소 추정) 차분 관리

### 2.3 정제·가공 항목
1. 주소 정규화 (도로명/지번 혼재, 층·호수 제거, 오타)
2. **지오코딩** → 위경도 생성
   - 1순위: 카카오 로컬 주소검색 API (무료 쿼터 넉넉)
   - 2순위: 브이월드(국토부) 지오코더 / 네이버클라우드 Geocoding
   - Google Geocoding 결과는 캐싱 제한(최대 30일)이 있어 **저장용 좌표로 부적합**
3. 업종 코드화 (한식/중식/일식/경양식/분식/이미용/세탁/기타 등 표준 분류표 자체 정의)
4. 메뉴·가격 정규화 (“7,000원”, “7000”, “7천원” → 정수), 대표가격(최저가) 산출
5. 전화번호 정규화 (매칭 키로 사용)
6. 자체 고유키 생성 (업소명+주소 해시 등; 원천에 ID가 없음)

---

## 3. 외부 플랫폼 연계 검토

### 3.1 업소 매칭(엔티티 매칭) — 핵심 공정
공공데이터 업소 ↔ Google place_id / 카카오 place_id / 네이버 링크를 연결하는 작업.

- 방법: 좌표 반경(예: 100m) + 업소명 유사도 + 전화번호 일치 → 점수화
- 결과 저장: `google_place_id`(영구 저장 허용), `kakao_place_id`, `naver_link`, `match_score`, `match_status(자동확정/검수필요/실패)`
- 저신뢰 매칭은 **관리자 검수 큐**로 → 검수 전에는 외부 평점 미노출
- 이유: 동명 업소·이전·폐업이 많아, 잘못 매칭되면 “착한가게에 다른 가게 악평”이 노출되는 치명적 오류 발생

### 3.2 Google (Places API New)
| 필드 | SKU 등급 |
|---|---|
| googleMapsUri | Pro |
| rating, userRatingCount, regularOpeningHours | Enterprise |
| reviews, reviewSummary, generativeSummary | Enterprise + Atmosphere |
| photos (ID) | Essentials(ID) + 사진 별도 과금 |

정책 제약:
- place_id 외 콘텐츠(평점·리뷰·사진·영업시간 등) **사전수집·저장 금지** → 상세 화면 열 때 **실시간 호출**
- 사진·리뷰는 **작성자 표기**(이름/아바타/프로필 링크) 필수
- 구글 지도가 아닌 지도 위에 표시할 경우 **Google Maps 로고** 표기 필수
- 따라서 목록(수십 건)마다 평점을 부르면 비용 폭증 → **목록에는 공공데이터 정보만, 상세 진입 시 구글 정보 호출**이 기본 설계
- 대안: Google **Places UI Kit / Place Details 요소**(구글이 렌더링, 정책 준수 내장) 사용 검토

첨부 화면의 “Closed · Opens 6:30 AM Fri”, “4.9”, AI 요약 문구는 구글 데이터(영업시간·평점·요약) 기반으로 보임.
- 요약 문구를 **구글 리뷰를 저장해 자체 LLM으로 요약**하는 방식은 저장 금지 조항과 충돌 소지 → **비권장**
- 대안: ① 구글 제공 reviewSummary/generativeSummary를 출처표기와 함께 그대로 표시(한국어 제공 범위 확인 필요) ② 공공데이터(메뉴·가격·업종)만으로 자체 요약 문구 생성 (“기본 김밥 2,000원” 같은 문장은 이것으로 충분)

### 3.3 네이버
- 지역검색 API(`/v1/search/local.json`): title, link, category, address, roadAddress, mapx/mapy — **평점·리뷰·사진 없음**, 최대 5건
- 플레이스 리뷰/사진 크롤링: 이용약관 위반, 부정경쟁방지법·저작권(DB권) 리스크 → **하지 않음**
- 가능한 연계:
  - 네이버 지도 앱/웹 **딥링크**(“네이버에서 리뷰 보기”, 길찾기)
  - **블로그 검색 API**로 “업소명 + 지역” 후기 목록(제목·요약·링크) 표시 — 공식 API라 허용 범위 내, 단 광고성 글 필터 필요
  - 이미지 검색 API는 이미지 저작권 문제로 비권장

### 3.4 카카오
- 로컬 API: 주소→좌표(지오코딩), 키워드 장소검색(place_url, id) — 평점 없음
- 용도: 지오코딩 1순위, 매칭 보조, **카카오맵 길찾기 딥링크**

### 3.5 자체 평가(중장기)
- 외부 평점에 기대지 않는 자체 별점/한줄평/“가격 아직 맞아요/올랐어요” 제보
- 필요: 로그인(소셜), 신고·검수, 개인정보처리방침 → MVP 이후 단계

---

## 4. 지도 엔진 선택

| 옵션 | 장점 | 단점 |
|---|---|---|
| A. 구글 지도 | 구글 평점과 한 화면, 로고 이슈 없음 | 국내 지도 상세도·길찾기(도보/자동차) 제약, 호출 비용 |
| B. 네이버 지도 | 국내 사용자 친숙, 지도 품질 | 구글 콘텐츠 표시 시 Google 로고 필수 |
| C. 카카오맵 | 무료 쿼터 큼, 지오코딩과 일원화 | B와 동일 |

**권장: B 또는 C를 기본 지도로 + 상세 패널에 Google 평점/사진(로고·출처 표기)**. 길찾기는 네이버/카카오 딥링크.
→ 사용자가 “구글맵 연동”을 필수로 원하면 A도 가능하나, 국내 길찾기는 딥링크로 보완.

---

## 5. 시스템 구성(안)

```
[data.go.kr API/CSV] ─분기 배치─▶ [ETL: 정제·지오코딩·매칭] ─▶ [DB: PostgreSQL + PostGIS]
                                        │                           │
                             [관리자 검수 화면]                      ▼
                                                            [자체 API 서버]
                                                  (검색·필터·반경조회·외부API 프록시)
                                                            │         │
                                        Google Places ◀─────┘         │
                                        Naver 블로그검색 ◀─────────────┘
                                                            ▼
                                          [반응형 웹/PWA (모바일·PC)]
                                                  └▶ (선택) 앱스토어 래핑
```

- 프론트: Next.js(React) 반응형 + PWA
- 백엔드: Next.js API Route 또는 별도 Node/Python 서버
- DB: PostgreSQL + PostGIS (반경검색·클러스터링) / 대안 Supabase
- 외부 API 키는 **서버 프록시**로 숨김, 호출량 제한(레이트리밋), 사용자당 쿼터
- 배포: Vercel/클라우드 + 도메인 / 공공기관 운영이면 G-클라우드 등 별도 검토

---

## 6. 화면 구성(안)

### 모바일
1. 상단: 검색창 + 필터칩(지역·업종·가격대·영업중)
2. 지도(마커 클러스터) + 하단 **바텀시트 목록** (끌어올리면 전체 목록)
3. 카드: 대표사진(있으면) · 업소명 · 업종 · 대표메뉴/가격 · 거리 · (상세 진입 후) 평점
4. 상세 화면: 첨부 제미나이 화면 구성 확장
   - 헤더: 사진, 업소명, 평점(구글, 로고), 업종, 영업상태
   - 요약 문구(공공데이터 기반 + 구글 요약)
   - **착한가격 메뉴표**(메뉴1~4/가격1~4) ← 이 앱의 핵심 정보
   - 전화 걸기 / 네이버·카카오 길찾기 / 공유 / 즐겨찾기
   - 구글 리뷰(작성자 표기), 네이버 블로그 후기 목록
   - 데이터 기준일 · “정보 오류 신고”

### PC
- 좌측 목록(필터 포함) 350~400px | 중앙 지도 | 우측(또는 오버레이) 상세 패널
- 목록 hover ↔ 지도 마커 하이라이트 연동

### 조회 조건
- 지역: 시도 → 시군구 (공공데이터 값 기준 계층)
- 업종: 정규화 분류
- 가격: “○원 이하 메뉴가 있는 곳”
- 내 주변: GPS + 반경(500m/1km/3km)
- 정렬: 거리순 / 가격순 / 평점순(평점순은 비용 이슈로 매칭된 곳만, 제한적)

---

## 7. 비용 검토 포인트
- Google Maps Platform은 2025년부터 SKU별 월 무료 사용량 구조 (등급 높을수록 무료량 적음, 단가 높음) → **최신 단가표로 재계산 필요**
- 비용 통제 설계
  - 목록에는 구글 호출 없음, 상세 진입 시 1회
  - 필드 마스크 최소화 (리뷰가 필요 없으면 Atmosphere 제외)
  - 일일 예산 상한 + 초과 시 구글 영역 숨기고 “구글에서 보기” 링크로 대체
- 카카오/네이버 지도·검색: 무료 쿼터 내 운영 가능성 높음 (쿼터 재확인)

---

## 8. 차별성 검토 (중요)
- 2025년 7월부터 행안부가 **네이버지도·카카오맵·티맵에 착한가격업소 정보 노출** 중
- 공식 사이트 goodprice.go.kr, 민간 “착한가게” 앱 등 이미 존재
- 따라서 이 앱의 차별 포인트를 명확히 할 필요:
  - 메뉴 가격 기준 필터(“5천원 이하 식사”)
  - 구글·네이버 평판을 한 화면에 모은 비교
  - 전국 통합 + 지역/업종 통계(지역별 평균 가격 등)
  - 가격 변동 사용자 제보(데이터 최신성 보완)

---

## 9. 리스크 및 대응

| 리스크 | 대응 |
|---|---|
| 매칭 오류로 다른 가게 평점 노출 | 점수 기반 자동확정 + 검수 큐, 저신뢰는 미노출 |
| 지오코딩 실패 | 다중 지오코더 fallback, 실패 목록 수기 보정 |
| 가격 정보 노후화(분기 갱신) | 기준일 표시, 사용자 제보 |
| 폐업·지정취소 | 분기 차분 비교, 구글 businessStatus 확인 |
| 구글 비용 폭증 | 상세 진입 시만 호출, 예산 상한 |
| 약관 위반 | 구글 콘텐츠 저장 금지 준수, 네이버 크롤링 금지 |
| 공공기관 명의 운영 시 | 공공 앱 신규개발 사전협의·보안성 검토 등 별도 절차 확인 |

---

## 10. 단계별 추진안

| 단계 | 내용 | 산출물 |
|---|---|---|
| 0. PoC (검증) | CSV 수집 → 지오코딩 성공률, 샘플 300~500건 구글/카카오 매칭률 측정, 구글 호출 단가 실측 | 성공률·비용 수치 보고 |
| 1. MVP | 공공데이터 + 지도 + 지역/업종/가격 필터 + 상세(메뉴표·전화·길찾기 딥링크), 반응형 | 웹 서비스 |
| 2. 평판 연계 | 구글 평점·사진·리뷰(상세 진입 시), 네이버 블로그 후기, 매칭 검수 관리자 | 확장 |
| 3. 고도화 | 즐겨찾기, 사용자 제보/리뷰, PWA 설치·앱스토어 래핑, 통계 대시보드 | 확장 |

---

## 11. 진행 전 결정 필요 사항
1. 운영 주체: 개인/민간 프로젝트인가, 행정안전부(공공기관) 공식 서비스인가?
2. 기본 지도: 구글 / 네이버 / 카카오 중 선택
3. 구글 리뷰 원문까지 필요한가, 평점·사진까지만인가 (비용 차이 큼)
4. 배포 형태: 웹(PWA) 우선 vs 처음부터 앱스토어 등록
5. 대상 업종: 음식점만인가, 이미용·세탁 등 전 업종인가
6. 예산 범위(월 API 비용 상한)

---

## 12. [추가 검토] 무료 구성 · 개발사례(비서비스) 기준

전제: 상용 서비스가 아닌 "공공데이터 + 지도 연계 개발 사례" 제작, 비용 0원 목표.

### 12.1 카카오 vs 네이버 (무료 관점)
| 항목 | 카카오 | 네이버 |
|---|---|---|
| 가입/결제수단 | 카카오 개발자 계정, 카드 불필요 | 지도·지오코딩은 네이버클라우드(NCP) 가입 + 결제수단 등록 필요 |
| 무료량 | 로컬 API 종류별 일 10만 건, 월 통합 300만 건 | 2025년 무료 이용량 정책 변경(확인 필요) |
| 지오코딩 | 주소검색 API 무료 | NCP Geocoding (과금 대상 가능) |
| 장소 ID | 키워드 검색 시 place id + place_url 제공 → **조인 키 확보 가능** | 지역검색 API에 장소 ID 없음 → 이름/주소로만 연결 |
| 후기 | place_url(카카오맵 장소 페이지: 별점·후기) 링크 | 블로그 검색 API(개발자센터, 무료)로 후기 글 목록 |

**결론: 기본 지도·지오코딩·장소 매칭 = 카카오 / 후기 보강 = 네이버 검색 API(블로그) / 구글 = 선택(쿼터 상한 설정)**

### 12.2 네이버 플레이스 후기 조인
- 공식 API로 플레이스 ID·후기를 받을 방법 없음
- 플레이스 페이지 크롤링: 약관·robots 위반, 데이터 부정사용(부정경쟁방지법) 등 민사 리스크, 봇 차단으로 기술적으로도 불안정 → 공개 사례로는 부적합
- 대체안
  1. 네이버 블로그 검색 API: "업소명 + 시군" 검색 → 후기 제목·요약·링크 표시 (공식, 무료)
  2. 네이버 지도 검색 링크: 업소명+주소로 네이버 지도 검색 페이지 이동 → 사용자가 플레이스 후기 직접 확인
  3. 네이버 지역검색 API: 좌표·카테고리로 동일 업소 확인(교차검증)용

### 12.3 구글을 무료로 쓰는 방법
- 2025년 3월부터 SKU별 월 무료량: Essentials 1만 / Pro 5천 / Enterprise 1천 건
- 평점(Enterprise)·리뷰(Enterprise+Atmosphere)도 월 1천 건까지 무료 → 데모 수준이면 충분
- Cloud Console에서 API별 **일일 할당량 상한**(예: 30건/일)을 걸면 초과 과금 차단
- 결제 계정(카드) 등록은 필요
- 키 없이 쓰는 Google Maps URL 링크("구글에서 보기")는 완전 무료

## 13. [확정 방향] 카카오 매핑 + 네이버/구글 "링크 연계" 구조

### 13.1 개념
공공데이터(착한가격 메뉴·가격) ⨝ 카카오(좌표·장소ID) = 앱 자체 데이터
→ 후기·평점은 **각 플랫폼 원본 페이지로 링크 이동** (앱이 콘텐츠를 가져오거나 저장하지 않음)

### 13.2 플랫폼별 링크 방식
| 플랫폼 | 연결 키 | 링크 | 정확도 |
|---|---|---|---|
| 카카오맵 | 카카오 장소 ID (매칭 시 확보) | place_url (장소 상세: 별점·후기) | 높음 (ID 직접) |
| 구글맵 | 구글 place_id (Text Search 'IDs Only' 호출로 확보, 영구 저장 허용) | Google Maps URL `?api=1&query=업소명+주소&query_place_id=ID` | 높음 (ID 확보 시) / ID 없으면 검색 링크 |
| 네이버 플레이스 | 공식 ID 획득 경로 없음 | 네이버 지도 검색 링크(업소명+주소), 모바일은 네이버지도 앱 스킴 | 중간 (검색 결과 첫 항목 의존) |

### 13.3 법적 판단
- 일반 하이퍼링크/딥링크: 콘텐츠 복제·저장이 없어 리스크 낮음
- 하지 않을 것: 크롤링, 네이버 페이지 iframe 삽입(프레이밍), 로고 임의 변형
- 버튼은 텍스트("네이버에서 후기 보기") 또는 각사 브랜드 가이드 준수 로고 사용
- 구글 place_id 확보용 API 호출 시에도 place_id 외 콘텐츠 저장 금지

### 13.4 "후기 모아보기" UX
- 상세 화면에 [카카오 후기] [네이버 후기] [구글 후기] 버튼 3개
- PC: 새 탭으로 열기 (여러 개 동시 팝업은 브라우저가 차단 → 버튼별 개별 열기)
- 모바일: 각 앱 설치 시 앱으로, 미설치 시 웹으로 fallback
- (선택) 구글 Maps Embed API(무료)로 상세 화면 내 구글 장소 카드 삽입 검토

### 13.5 경량 아키텍처 (서버 없음)
- 전처리(1회/분기, 로컬 PC 스크립트): 공공데이터 CSV → 카카오 좌표·장소ID 매칭 → (선택) 구글 place_id → 시도별 JSON 파일 생성
- 앱: 정적 웹(반응형) + 카카오 지도 JS SDK + 시도별 JSON 로드 → 필터·지도·상세·링크
- 호스팅: GitHub Pages / Vercel 등 무료 정적 호스팅
- 카카오 JS 키는 도메인 제한으로 노출 문제 없음, REST 키는 전처리 스크립트에서만 사용

## 14. 현재 위치 기반 거리 표시

### 14.1 위치 확보
| 환경 | 방법 | 정확도 |
|---|---|---|
| 모바일 | 브라우저 Geolocation API (GPS) — 권한 허용 팝업 | 수~수십 m |
| PC | 동일 API (Wi-Fi/IP 기반) | 수백 m~수 km 오차 가능 |
| 공통 보완 | 수동 설정: 지도 클릭 / 주소·장소 검색(카카오 JS SDK services) / 내 위치 핀 드래그 | 사용자 지정 |
| 권한 거부 시 | 시도·시군 선택 모드로 전환 | — |

- HTTPS 필수(정적 호스팅 기본 제공), 마지막 위치는 브라우저 localStorage에만 저장(선택)

### 14.2 거리 계산·표시
- 브라우저에서 하버사인 공식으로 직선거리 계산 (API 호출·비용 없음)
- 표시: "직선 350m" / "1.2km", 목록 거리순 정렬, 반경 필터(500m/1km/3km/5km), 지도에 반경 원
- 실제 도보·차량 경로 거리는 카카오맵/네이버지도 길찾기 링크로 위임

### 14.3 개인정보
- 위치를 서버로 전송·저장하지 않고 기기 안에서만 계산 → 수집 없음
- 화면에 "위치는 기기 안에서만 사용되며 저장·전송되지 않습니다" 안내
- 향후 실제 서비스화 시 위치정보법상 신고 대상 여부 별도 확인

## 15. 분기별 데이터 갱신 처리

### 15.1 전제
- 원천 데이터에 업소 고유ID가 없음 → 자체 키로 이전/신규 버전을 대조해야 함
- 자체 키 = 정규화(업소명) + 정규화(주소), 보조 키 = 정규화(전화번호)

### 15.2 처리 흐름
1. 수집: 새 CSV/API 전체 다운로드
2. 검증 게이트: 컬럼 구조 변경, 건수 급감(예: -10% 이상), 필수값 누락률 급증 시 중단
3. 정규화: 업소명·주소·전화·가격
4. 차분 분류 (이전 버전 대비)
   | 구분 | 판단 기준 | 처리 |
   |---|---|---|
   | 유지 | 키 동일, 내용 동일 | 기존 좌표·카카오ID·구글ID 재사용 (API 호출 없음) |
   | 정보변경 | 키 동일, 메뉴·가격·전화 변경 | 값만 갱신, 가격 이력 기록 |
   | 이전(주소변경) | 업소명+전화 동일, 주소 다름 | 재지오코딩·재매칭 |
   | 상호변경 | 주소+전화 동일, 업소명 다름 | 검수 목록 |
   | 신규 | 대응 레코드 없음 | 지오코딩·카카오/구글 매칭 |
   | 제외 | 새 데이터에 없음 | 앱에서 숨김(삭제하지 않고 이력 보관) |
5. 신규·이전 건만 API 호출 → 쿼터·시간 절약
6. 검수 목록 확인: 매칭 실패, 저신뢰 매칭, 상호변경 의심
7. 수기 보정 파일(overrides) 재적용 — 사람이 고친 값이 다음 분기에도 유지되도록 별도 파일로 관리
8. JSON 생성 + 데이터 기준일·버전 기록, 이전 버전 보관(롤백용)
9. 배포

### 15.3 식별자 유효성 점검
- 구글 place_id: 변경·만료될 수 있어 12개월 이상 된 ID는 재확인(IDs Only 호출)
- 카카오 장소ID: 기존 ID로 재조회 실패 시 재매칭

### 15.4 자동화(선택)
- GitHub Actions 월 1회: data.go.kr 수정일 확인 → 변경 시 스크립트 실행 → 차분 보고서 포함 PR 생성 → 검토 후 병합 = 자동 배포
- 수동 운영 시: 분기 1회 로컬 스크립트 실행 → 결과 보고서 확인 → 업로드

### 15.5 부가 활용
- 가격 이력으로 "가격 변동" 표시, 지역별 신규 지정·제외 추이 통계

## 16. 좌표의 다중 플랫폼 활용 및 좌표 보정 절차

### 16.1 좌표 활용 (WGS84 위경도 = 카카오·네이버·구글 공통)
- 마커 표시: 어느 지도 SDK든 동일 좌표 그대로 사용
- 단, 좌표 ≠ 장소ID. 각 플랫폼 "장소 페이지"로 정확히 연결하려면 좌표를 기준점으로 한 매칭 필요

| 플랫폼 | 좌표 활용 매칭 | 결과 |
|---|---|---|
| 카카오 | 키워드검색(업소명) + x,y + radius(예: 50m) | 카카오 장소ID → place.map.kakao.com/{id} |
| 구글 | Text Search(업소명) + locationBias(좌표 반경), IDs Only | place_id → 구글맵 장소 링크 |
| 네이버 | 지역검색(업소명+시군구) 결과 좌표와 거리 비교(검증용), 지도 링크에 중심좌표 지정 | 검색 링크 + "동일 업소 확인됨" 신뢰도 표시 |

- 네이버 지역검색 좌표는 정수형 표기 등 형식 차이 가능 → PoC에서 변환 확인
- 매칭 결과 필드: kakao_id, google_place_id, naver_verified, match_distance_m, match_score

### 16.2 좌표 오류 탐지
| 유형 | 탐지 방법 |
|---|---|
| 좌표 없음 | 사이트 데이터에 없는 업소(CSV 신규 등) |
| 행정구역 불일치 | 좌표가 소속 시군구 경계 밖 (행정구역 경계 GeoJSON, point-in-polygon) |
| 주소-좌표 불일치 | 주소를 카카오로 재지오코딩한 좌표와 거리 > 200m |
| 좌표 중복 | 여러 업소가 동일 좌표(시군청 등 기본값 입력 의심) |
| 장소 미존재 | 좌표 반경 내 카카오 장소 검색 결과에 해당 업소명 없음 |

### 16.3 보정 절차 (자동 → 수동 순)
1. 주소 정제(괄호·층·호 제거, 도로명/지번 분리) 후 카카오 주소검색
2. 실패 시 카카오 키워드검색(업소명 + 시군구)
3. 실패 시 브이월드/국토부 지오코더(도로명·지번)
4. 후보 간 거리 비교 → 합의되는 좌표 채택, 불일치 시 검수 대상
5. 수동 검수: 검수 목록(엑셀/간이 관리화면) + 지도 핀 드래그로 확정 → overrides 파일 저장(다음 갱신에도 유지)
6. 끝내 미확정: 지도 마커 제외, 목록에만 "위치 확인 중" 표시

- 기록 필드: coord_source(site / kakao_addr / kakao_place / vworld / manual), coord_confidence, verified_at
- 앱 내 "위치가 달라요" → 공식 업소정보 오류 신고 링크 안내

## 17. 업소 이전(주소 변경) 시 외부 장소ID 처리

### 17.1 플랫폼별 동작 (공식 보장 없음 → 재검증 전제)
| ID | 이전 시 일반적 동작 |
|---|---|
| goodprice 업소번호(bsshSn) | 같은 레코드 주소 수정이면 유지 추정. 관할 변경·재지정 시 새 번호 가능 |
| 구글 place_id | 업주가 비즈니스 프로필 주소 수정 시 유지되는 경우 많음. 새 등록 시 기존 ID는 '폐업/이전' 처리. place_id 자체도 변경·만료 가능(12개월 이상 된 ID 재확인 권장) |
| 카카오 장소ID | 이전 시 새 장소로 등록되는 경우가 많음(공식 문서 미확인) |
| 네이버 | ID 미보관(검색 링크) → 영향 없음 |

### 17.2 설계 원칙
- 기준 키는 자체 관리번호(GP#####). 외부 ID는 속성(검증일 포함)
- 외부 ID 이력 테이블: 관리번호, 플랫폼, 이전ID, 새ID, 변경일, 사유

### 17.3 감지 → 재검증 절차
1. 감지: 분기 갱신 차분에서 '이전'(업소명·전화 동일, 주소 상이) / 새 좌표와 기존 외부 장소 좌표 거리 > 200m / 플랫폼 영업상태 '폐업'
2. 재검증
   - 구글: 저장된 place_id로 조회 → 반환 ID·주소·영업상태 비교, 폐업/불일치 시 새 좌표 주변 재검색
   - 카카오: 새 좌표 반경 검색(업소명) → ID 다르면 교체
3. 교체 시 이력 기록, 판단 불가 시 검수 목록
4. 역방향: 정부 데이터엔 있는데 플랫폼상 폐업 → '폐업 의심' 표시 + 공식 오류신고 링크

## 참고 출처
- 공공데이터포털 행정안전부_착한가격업소 현황: https://www.data.go.kr/data/3045247/fileData.do
- 공공데이터포털 API 자동변환 보도자료: https://www.mois.go.kr/frt/bbs/type010/commonSelectBoardArticle.do?bbsId=BBSMSTR_000000000008&nttId=84567
- Google Places API 정책·출처표기: https://developers.google.com/maps/documentation/places/web-service/policies
- Google Places 데이터 필드(SKU): https://developers.google.com/maps/documentation/places/web-service/data-fields
- 네이버 지역검색 API 사용 예: https://velog.io/@minseokangq/Naver-%EC%A7%80%EC%97%AD-%EA%B2%80%EC%83%89-API-%EC%97%B0%EB%8F%99%ED%95%B4%EB%B3%B4%EA%B8%B0
- 지도앱 착한가격업소 노출(정책브리핑): https://www.korea.kr/news/policyNewsView.do?newsId=148929002
- 착한가격업소 공식: https://goodprice.go.kr/
